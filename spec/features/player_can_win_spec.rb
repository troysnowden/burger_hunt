RSpec.feature "Player can win", type: :feature do

  scenario "player moves to 'Win' when they click on the 'Correct' button on page5" do
    # visit ('game/page5')
    # click_link 'Right (correct)' 
    # expect(page).to have_current_path '/game/win'
  end
  # This test fails until I have made the Right (correct) button appear
  # option - the button is there, but pale? 
  # This has the risk of somebody clicking on it accidentally?

end